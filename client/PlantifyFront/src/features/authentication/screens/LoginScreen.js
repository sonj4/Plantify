import { StyleSheet, ScrollView, Text } from "react-native";

const LoginScreen = () => {
    return (
        <ScrollView>
            <Text>Login</Text>
        </ScrollView>
    )
}

export default LoginScreen;